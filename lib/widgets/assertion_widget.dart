import 'package:flutter/material.dart';

class AssertionWidget extends StatelessWidget {
  const AssertionWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("Assertion!!!");
  }
}
